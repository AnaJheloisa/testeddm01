import React from "react";
import { View,Text } from "react-native";

export default function Heloisa(){
    return(
        <view>
            <text>Correção da Prova DDM</text>
        </view>
    );
}